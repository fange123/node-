// * IP地址和域名是一一对应的关系,这种对应关系被存放在《域名服务器（NDS）》中
// * 应该ip地址不好记忆，所以给它取个名字方便记忆--域名

// * 在开发测试期间，127.0.0.1的域名是 localhost

// * 在一个服务器中，有成百上千个web服务，每个服务都应该对应一个端口号
// * http 默认80端口，在url中，80端口是可以被省略的
// * https 默认443端口
